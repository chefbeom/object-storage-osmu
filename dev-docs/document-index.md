# OSMU Document Index

This index points to the useful project documents for the current prototype. The root `README.md` is the fastest starting point for running and verifying the project.

## Start Here

1. `../README.md` - prototype overview, run commands, verification commands.
2. `PRODUCT_REQUIREMENTS.md` - product goal, MVP scope, final target scope.
3. `PROJECT_MEMORY.md` - long-term project memory and direction.
4. `prototype-status.md` - current implemented scope, latest verification, blockers, and next work.
5. `feature-inventory.md` - current feature map, implemented scope, gaps, fixes, and next development direction.
6. `mvp-release-checklist.md` - MVP v0.1 release gate checklist and go/no-go rules.
7. `development-roadmap.md` - roadmap from MVP to product.

## Engineering Design

- `system-architecture.md` - high-level architecture.
- `api-spec.md` - REST and S3-compatible API notes.
- `s3-compatibility.md` - supported, partial, and unsupported S3-compatible API/client matrix.
- `openapi-mvp.json` - machine-readable MVP REST/S3 API contract snapshot.
- `database-design.md` - MariaDB schema and metadata design.
- `backend-design.md` - Spring Boot backend design.
- `frontend-design.md` - Vue portal design, including dashboard operations readiness visibility.
- `storage-profile.md` - bucket-level Performance/Standard/Durable request, approval, MariaDB, UI, and MinIO pool/parity design.
- `local-dev-env.md` - local Docker/MariaDB/MinIO environment configuration.

## Operations

- `security-design.md` - auth, access keys, permissions, security policy.
- `iam-rbac-matrix.md` - role, endpoint, dashboard panel, and operation permission matrix.
- `kubernetes-rbac-matrix.md` - Kubernetes ServiceAccount, token automount, storage expansion runner, and cluster RBAC boundary matrix.
- `backup-recovery.md` - backup and recovery direction.
- `operation-monitoring.md` - health checks, backup readiness status, logs, metrics, monitoring, and Alertmanager/Grafana threshold target contract.
- `deployment-strategy.md` - Docker Compose, Kubernetes, Helm direction.
- `minio-pool-expansion.md` - MinIO server pool-based capacity expansion strategy and Storage Expansion Manager direction.
- `storage-profile.md` - MinIO pool/parity intent mapping for Storage Profile.
- `../infra/k8s/README.md` - Kubernetes draft manifest guide.

## Testing

- `test-strategy.md` - test strategy and quality gates.
- `test-cases.md` - detailed test cases.
- `../scripts/verify-local.ps1` - local static/unit/build/backend verification, including explicit UTF-8 PowerShell script parse checks, UTF-8 latest JSON evidence reads, encoding hygiene for docs, source, scripts, durable MVP finalize plan-only preservation, MVP completion/preflight handoff, and a final latest operations evidence refresh with freshness and selected-action scope assertions.
- `../scripts/verify-doc-encoding-hygiene.ps1` - verify `README.md`, `dev-docs`, frontend source, backend main source, and PowerShell scripts stay free of UTF-8 replacement characters, C0/DEL/C1 controls, and CJK unified/compatibility ideographs that usually signal mojibake in this English/Hangul project surface.
- `../scripts/verify-doc-encoding-hygiene-self-test.ps1` - self-test the encoding hygiene guard with clean, UTF-8 replacement character, C0 control, C1 control, CJK unified/compatibility ideograph, and `-AllowCjkIdeographs` fixtures.
- `../scripts/verify-dev-doc-file-references.ps1` - verify root `README.md`, `dev-docs`, and `infra` Markdown references to `scripts/*.ps1` and `.github/workflows/*.yml|yaml` still resolve to committed workspace files.
- `../scripts/verify-development-roadmap.ps1` - verify the roadmap keeps sequential numbered sections, the current B2B operations priority, S3 replacement boundary, next implementation order, and 2026-06-30 readiness snapshot; when latest `.osmu-run` MVP completion, operations readiness, evidence plan, artifact collection, evidence handoff, or readiness convergence JSON exists, the verifier cross-checks the snapshot line, durable preflight handoff, total/check counts, source pending category summary/counts, pending remediation count/list, evidence-plan remediation coverage, workflow run-id security finalizer hint count/supplemental inputs, security finalizer input checklist and missing run-id inputs, handoff bottleneck/missing workflow run count/browser dispatch checklist, and convergence bottleneck/Kubernetes sync stale flag against that evidence.
- `../scripts/verify-prototype-status.ps1` - verify the prototype status document stays current on local durable MVP readiness, the 2026-06-30 operations readiness snapshot, production/B2B target-evidence gaps, S3 replacement boundary, enterprise auth, data-flow, commercial, and operations evidence chain scope; when latest `.osmu-run` MVP completion, operations readiness, evidence plan, artifact collection, evidence handoff, or readiness convergence JSON exists, the verifier cross-checks the snapshot line, durable preflight handoff, total/check counts, pending category summary/counts, pending remediation count/list, evidence-plan remediation coverage, workflow run-id security finalizer hint count/supplemental inputs, security finalizer input checklist and missing run-id inputs, handoff bottleneck/missing workflow run count/browser dispatch checklist, and convergence bottleneck/Kubernetes sync stale flag against that evidence and writes `.osmu-run/latest-prototype-status.json` with structured durable preflight result, next action, next action command, blocking actions, evidence-plan remediation coverage, workflow run-id security finalizer hint summary, and handoff browser dispatch checklist snapshots.
- `../scripts/verify-commercial-readiness.ps1` - verify commercial readiness docs keep chargeback, approval, native bridge, evidence, and remaining vendor-specific scope boundaries current using strict UTF-8 reads.
- `../scripts/verify-image-signing-policy.ps1` - verify image signing policy scope, release/commit tag expectations, and GitHub OIDC keyless signing guidance using explicit UTF-8 reads for the policy and workflow inputs.
- `../scripts/verify-container-hardening.ps1` - verify container hardening Dockerfile, nginx, Kubernetes, and Helm controls such as non-root runtime, health checks, and minimal runtime assumptions using explicit UTF-8 reads.
- `../scripts/verify-tls-ingress.ps1` - verify TLS ingress manifests, Helm values, and documentation keep the intended certificate/ingress boundary using explicit UTF-8 reads for manifest, values, and template inputs.
- `../scripts/verify-secret-rotation-policy.ps1` - verify secret/certificate rotation policy docs, examples, and release gates keep the no-secret evidence boundary using explicit UTF-8 reads for policy, deployment, security, Helm, manifest, and evidence-script inputs.
- `../scripts/verify-backup-restore-drill.ps1` - verify backup/restore drill scripts, manifests, and documentation remain wired for plan/live DR evidence using explicit UTF-8 reads for docs and helper scripts.
- `../scripts/verify-prometheus-observability.ps1` - verify Prometheus observability manifests and docs stay aligned with the monitoring draft scope using explicit UTF-8 reads for backend, Kubernetes, and Helm inputs.
- `../scripts/verify-monitoring-artifacts.ps1` - verify Prometheus/Grafana/Alertmanager artifacts and operation docs remain in sync using explicit UTF-8 reads for monitoring artifacts and operation docs.
- `../scripts/verify-prometheus-operator-draft.ps1` - verify the Prometheus Operator draft manifests, docs, and release scope guard using explicit UTF-8 reads for Kubernetes, Helm, kustomization, and monitoring README inputs.
- `../scripts/verify-helm-chart.ps1` - verify Helm chart values/templates, security defaults, operations report mounts, and release documentation using explicit UTF-8 reads for chart files and template scans.
- `../scripts/verify-migrations.ps1` - verify Flyway migration ordering, version continuity, and schema-script naming hygiene.
- `../scripts/write-migration-rollback-plan.ps1` - write a Flyway forward-only migration rollback plan with backup, restore, smoke, and compensating-migration stages.
- `../scripts/verify-migration-rollback-plan.ps1` - verify the migration rollback plan shape, required stages, backup-artifact requirement, and no-secret reference policy using explicit UTF-8 reads for generated JSON/Markdown plan evidence.
- `../scripts/verify-metadata-index-coverage.ps1` - statically verify migration-backed index prefixes for high-volume metadata, data-flow, audit, storage expansion, and chargeback retry query paths using explicit UTF-8 reads for migration SQL inputs.
- `../scripts/write-mariadb-query-plan-evidence.ps1` - write plan-only, operator-collected, or live `EXPLAIN FORMAT=JSON` evidence for high-volume MariaDB query paths without storing database passwords.
- `../scripts/verify-mariadb-query-plan-evidence.ps1` - self-test MariaDB query plan evidence shape, expected-index pass fixtures, and wrong-index failure fixtures using explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-object-list-query-pushdown.ps1` - statically verify MariaDB object list/search/tag/trash query pushdown for search, cursor, limit, and tag lookup paths using an explicit UTF-8 read for the repository source.
- `../scripts/verify-prototype-prerequisites.ps1` - check Java, Node/npm, Docker, real S3 clients, and runtime/frontend/database/storage endpoints before deeper prototype verification.
- `../scripts/verify-prototype-release.ps1` - one-command MVP release gate that combines prerequisites, build verification, backend tests, runtime smoke, seeded demo smoke, and S3 smoke.
- `../scripts/write-mvp-audit.ps1` - write a human-readable MVP audit from the latest release evidence report plus optional durable gate/finalizer reports, reading release and gate JSON through strict UTF-8.
- `../scripts/write-mvp-release-decision.ps1` - write lightweight demo and durable pilot go/no-go decisions from the latest release evidence report plus optional durable gate/finalizer reports, reading release and gate JSON through strict UTF-8.
- `../scripts/write-mvp-release-notes.ps1` - write lightweight or durable demo release notes from the latest release evidence, durable gate, audit, and decision reports using strict UTF-8 reads for source JSON/Markdown inputs.
- `../scripts/write-mvp-demo-package-notes.ps1` - write MVP demo package handoff notes from completion, demo readiness, durable gate/finalizer/preflight, and release artifacts using strict UTF-8 reads for source JSON/Markdown evidence; when MVP completion evidence exists, package status mirrors that completion classification.
- `../scripts/write-durable-release-artifacts.ps1` - convert a passed durable demo gate report plus backend test evidence into synchronized durable release JSON, audit, decision, release notes, and artifact verification using strict UTF-8 reads; preserves selected S3 client and durable preflight report evidence from the gate report.
- `../scripts/finalize-durable-mvp-demo.ps1` - one-command durable MVP finalization wrapper for Docker-ready machines; runs durable preflight, backend tests, durable gate, durable release artifact generation, and hard readiness verification. `-EnableRealMultipartEvidence` passes through to the durable gate and records the browser multipart evidence JSON/Markdown paths; `-PlanOnly` writes `.osmu-run/latest-durable-mvp-finalize-plan.*` by default so it does not overwrite the latest ready finalizer evidence unless `-ReportPath` or `-SummaryPath` is explicitly supplied.
- `../scripts/verify-durable-mvp-finalize-plan.ps1` - self-test durable finalize plan-only evidence hygiene without Docker; verifies default `-PlanOnly` writes the `latest-durable-mvp-finalize-plan.*` reports and preserves `latest-durable-mvp-finalize.*`, while explicit report paths do not claim default ready-evidence preservation.
- `../scripts/verify-mvp-completion.ps1` - verify the current local durable MVP completion state from demo readiness, durable gate, durable finalizer, latest durable preflight, release artifacts, demo package status alignment, and status documents; writes `.osmu-run/latest-mvp-completion.*` with durable preflight `result`, `nextActionCommand`, and `blockingActions[]` handoff when `.osmu-run/latest-durable-demo-preflight.json` exists, while keeping production/B2B operations readiness separate. Use `-FailIfLocalMvpNotReady` only as a hard gate after a Docker-ready durable finalizer report refreshes the latest finalizer path.
- `../scripts/verify-mvp-release-checklist.ps1` - verify the MVP release checklist keeps current operations evidence paths and bottleneck snapshot lines in sync with latest MVP completion/durable preflight handoff, operations readiness, evidence plan remediation coverage, workflow run-id, workflow run-id security finalizer hint summary, artifact collection, evidence handoff browser dispatch checklist, and readiness convergence JSON.
- `../scripts/write-operations-readiness.ps1` - write a production/B2B operations readiness JSON and Markdown report from release scope, RBAC/security/static controls, storage expansion finalizer evidence, storage backend telemetry evidence, data-flow storage transition target evidence, Kubernetes HA/DR readiness, Kubernetes DR finalizer evidence, image signing evidence, container scan/SBOM evidence, secret rotation evidence, commercial integration evidence, commercial approval evidence, enterprise auth smoke evidence, and operations handoff package evidence; enterprise auth `result=passed` promotion requires typed integer summary counts with passCount>0 and fail/block/planned counts at zero, image/container security remediation points operators to GitHub OIDC keyless signing, release/commit tag verification, Trivy CRITICAL/HIGH scans, commit-SHA image tags, and SPDX SBOM metadata, and the handoff package gate requires typed boolean confirmations, strict typed readiness/convergence snapshots with finalizer failed/gap counts at zero and Kubernetes report sync source `ready`, non-empty evidence refs, typed target evidence snapshots, synchronized summary counts, and complete PASS check rows; top-level `totalCount`/`checkCount` mirror the authoritative check array size, `pendingCategorySummary`/`pendingCategoryCounts` summarize non-passing checks by category, `pendingRemediationCount`/`pendingRemediations[]` mirror pending checks with command/workflow/workflow-command/note metadata, and pending live/security/storage-telemetry/data-flow/secret-rotation/commercial/enterprise-auth/package checks include remediation command/workflow metadata where applicable, including the dedicated storage backend telemetry, data-flow storage plan, cluster network access review, Helm values hardening, and support escalation handoff evidence workflows. It reads evidence JSON inputs through explicit UTF-8.
- `../scripts/verify-operations-readiness.ps1` - regenerate and verify the operations readiness artifact shape, required pending evidence checks, top-level pending remediation summary, enterprise auth passed/scope-out promotion guards, remediation metadata without requiring the target cluster to be ready, and explicit UTF-8 reads for generated JSON/Markdown readiness evidence.
- `../scripts/write-secret-rotation-evidence.ps1` - record target-environment secret/certificate rotation evidence as `.osmu-run/latest-secret-rotation-evidence.*` with external references and booleans only; it rejects credential-shaped references and never stores secret values.
- `../scripts/verify-secret-rotation-evidence.ps1` - self-test the secret rotation evidence writer, passed evidence shape, secret-like reference rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-cluster-network-access-review-evidence.ps1` - self-test cluster network access review evidence, timestamp ordering, static NetworkPolicy snapshot parsing, credential-shaped reference rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-helm-values-hardening-evidence.ps1` - self-test Helm values hardening evidence, static hardening snapshots, tampered values rejection, timestamp ordering, credential-shaped reference rejection, and explicit UTF-8 reads for generated JSON/Markdown and fixture values inputs.
- `../scripts/write-enterprise-auth-smoke-plan.ps1` - record plan-only, live target, or commercial scope-out enterprise auth smoke evidence as `.osmu-run/latest-enterprise-auth-smoke.*`; passed target evidence carries typed integer summary counts with passCount>0 and fail/block/planned counts at zero, reads optional OIDC claim/JIT JSON request bodies through explicit UTF-8, rejects token or credential-like input fields before request execution, and never writes admin passwords, LDAP passwords, OIDC code/state, tokens, client secrets, or raw claim JSON to evidence.
- `../scripts/verify-enterprise-auth-smoke-plan.ps1` - self-test the enterprise auth smoke writer, plan-only and scope-out shapes, secret redaction, credential-like scope-out rejection, unsafe token-bearing claim input rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-storage-backend-telemetry-evidence.ps1` - record MinIO pool/server/drive telemetry evidence from `mc admin info --json` file input or explicit `-Execute`; reads prepared admin-info JSON through explicit UTF-8 and stores summaries, SHA-256, and external references only, never raw admin output or credentials.
- `../scripts/verify-storage-backend-telemetry-evidence.ps1` - self-test storage backend telemetry evidence shape, pool/node/capacity parsing, secret-like input rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-minio-bucket-cors.ps1` - verify MinIO bucket CORS from `mc cors info <alias>/<bucket>` or a collected CORS XML file; records only normalized methods, origins, allowed headers, exposed headers, max-age, and checks needed by browser multipart upload.
- `../scripts/verify-minio-bucket-cors-self-test.ps1` - self-test MinIO bucket CORS verification with plan-only, passed, and failed fixtures.
- `../scripts/write-commercial-integration-evidence.ps1` - record target-environment notification webhook, Slack, EMAIL SMTP relay, generic payment webhook, CARD/BANK/TAX/ERP payment webhook profile handoff, configurable native bridge readiness, and sanitized payment-provider adapter readiness evidence as `.osmu-run/latest-commercial-integration-evidence.*` without credentials, raw provider responses, or vendor-specific fixed SDK/schema processor implementation claims; reads readiness snapshot JSON through explicit UTF-8, and readiness snapshot count fields must be typed integers and readiness/profile fields must be typed booleans.
- `../scripts/verify-commercial-integration-evidence.ps1` - self-test the commercial integration evidence writer, payment-provider adapter readiness snapshot parsing, passed evidence shape, string-typed count/boolean rejection, credential-like reference rejection, unsafe raw provider/customer snapshot rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-commercial-approval-evidence.ps1` - record final pricing, terms, support SLA, license agreement, legal approval, pilot contract boundary approval, and sanitized billing pricing proposal commercial approval evidence as `.osmu-run/latest-commercial-approval-evidence.*` with references, status metadata, and typed booleans only, reading optional approval snapshot JSON through explicit UTF-8.
- `../scripts/verify-commercial-approval-evidence.ps1` - self-test the commercial approval evidence writer, pricing proposal approval snapshot parsing, passed approval shape, string-typed approval flag rejection, price/raw-contract exclusion, unsafe raw commercial snapshot rejection, credential-like reference rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-chargeback-closeout-evidence.ps1` - record target chargeback closeout evidence with typed invoice/payment/reconciliation/native-adapter snapshots and raw customer/payment/provider exclusion, reading optional closeout snapshot JSON through explicit UTF-8.
- `../scripts/verify-chargeback-closeout-evidence.ps1` - self-test chargeback closeout evidence, typed invoice/payment/reconciliation snapshots, native adapter readiness review, raw customer/payment/provider exclusion, target-period planned/pass boundaries, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-enterprise-auth-jit-rollback-evidence.ps1` - record target enterprise auth JIT rollback/runbook evidence with reduced smoke snapshot validation, raw-claim rejection, and credential-shaped reference rejection, reading optional smoke snapshot JSON through explicit UTF-8.
- `../scripts/verify-enterprise-auth-jit-rollback-evidence.ps1` - self-test enterprise auth JIT rollback/runbook evidence, passed smoke snapshot requirements, scope-out behavior, timestamp ordering, raw-claim rejection, credential-shaped reference rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-support-escalation-handoff-evidence.ps1` - self-test support escalation handoff evidence, document coverage checks, timestamp ordering, missing confirmation behavior, secret-like reference rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-handoff-package.ps1` - record target-environment operations handoff package evidence as `.osmu-run/latest-operations-handoff-package.*`, tying runbook, troubleshooting, rollback, support escalation, known gaps, commercial approval, reviewed enterprise auth smoke or scope-out, target evidence references, sanitized operations readiness/convergence snapshot summaries, sanitized data-flow storage plan/query-plan summaries, sanitized data-flow storage transition runbook summaries, sanitized commercial summaries, sanitized enterprise auth smoke summaries, and sanitized cluster network/Helm hardening summaries together without executing external commands or storing secrets, raw SQL, raw EXPLAIN, object keys, raw event messages, raw identity claims, raw remediation, raw provider/customer, contract, license, or payment content; required convergence snapshots must use typed integer finalizer counts and typed boolean Kubernetes sync readiness, MariaDB/dual-write data-flow plan snapshots must include passed typed query-plan summary fields, enterprise auth passed snapshots must include typed summary counts with passCount>0 and fail/block/planned counts at zero, and required target snapshot confirmations plus commercial count/approval fields must use typed values before a package can pass, and reads sanitized JSON snapshot inputs through explicit UTF-8.
- `../scripts/verify-operations-handoff-package.ps1` - self-test the operations handoff package writer, passed package shape, readiness/convergence/data-flow storage plan/data-flow storage transition runbook/commercial/enterprise-auth snapshot parsing, required snapshot failure, convergence snapshot string-boolean and missing-integer rejection, missing/string-count query-plan summary rejection, target snapshot string-boolean/count rejection, enterprise auth passed string-count rejection, credential-like reference/snapshot rejection, unsafe raw operations snapshot rejection, unsafe raw SQL data-flow snapshot rejection, unsafe raw price-table commercial snapshot rejection, and unsafe raw identity-claim enterprise auth snapshot rejection, and explicit UTF-8 reads for generated and mutated JSON/Markdown evidence.
- `../scripts/write-operations-evidence-plan.ps1` - convert pending operations readiness checks into an ordered evidence plan with per-action current detail, recommended/local/`gh workflow run` commands, operator placeholders, `run_live=true` approval flags, kubeconfig-secret requirements, source readiness total/check counts, source pending-remediation entry/action/missing-action coverage, action summary counts for action types, approvals, kubeconfig secrets, placeholders, unplanned checks, and source-compatible pending category counts. It reads the source readiness JSON through explicit UTF-8.
- `../scripts/verify-operations-evidence-plan.ps1` - self-test the evidence plan writer schema, current-detail/recommended-command handoff, placeholder extraction, approval flags, kubeconfig-secret detection, source count/category/remediation coverage propagation, unplanned-check handling, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/invoke-operations-evidence-plan.ps1` - turn the ordered evidence plan into a guarded plan-only or explicit `-Execute` invocation report; blocks unresolved placeholders, invalid known placeholder value shapes, missing operator approval, missing kubeconfig-secret confirmation, missing GitHub API token, and unsafe commands before dispatching live evidence workflows through GitHub CLI or the REST API. It reads the selected evidence plan JSON through explicit UTF-8.
- `../scripts/verify-operations-evidence-plan-invocation.ps1` - self-test the evidence plan invocation helper across blocked, confirmed, and selected-action plan-only runs with explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-invocation-unblock-plan.ps1` - read the guarded invocation report and generate an operator handoff for resolving blocked actions, including required confirmations, unresolved or invalid placeholders, per-action plan commands, and currently planned actions that can be run separately. It reads the invocation report JSON through explicit UTF-8.
- `../scripts/verify-operations-invocation-unblock-plan.ps1` - self-test invocation unblock planning across blocked and ready invocation fixtures with explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-operator-input-worksheet.ps1` - expand the invocation unblock plan and dispatch preflight into JSON, Markdown, and CSV operator-input worksheets, splitting repeated placeholders such as timestamps, references, latency metrics, counts, and base64 payloads into workflow-input-level rows without marking evidence as passed.
- `../scripts/verify-operations-operator-input-worksheet.ps1` - self-test the operator-input worksheet writer across input-free actions, required secrets, dispatch unavailability reasons, and ambiguous placeholder expansion using explicit UTF-8 reads for generated JSON/Markdown/CSV evidence.
- `../scripts/write-operations-dispatch-preflight.ps1` - verify dispatch readiness from the invocation unblock plan without executing workflows, including source readiness total/check counts, confirmations, placeholder values, known placeholder value shapes, workflow files, required GitHub secret names, optional GitHub CLI availability, and GitHub REST API execute-command previews when a repository slug is resolved. It reads workflow/unblock inputs through explicit UTF-8.
- `../scripts/verify-operations-dispatch-preflight.ps1` - self-test dispatch preflight across missing-input and ready fixtures with explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-workflow-run-id-plan.ps1` - derive `gh run list` commands, GitHub REST API query URLs and `-UseGitHubApi` command, browser workflow runs URLs, top-level run-id input hints, recommended command handoff, structured Security Evidence Finalizer readiness/required/missing run-id input hints plus `securityEvidenceFinalizerRunIdInputHints[]` supplemental source hints, and recommended latest successful workflow run ids from an invocation report, then build Security Evidence Finalizer and artifact collection plan follow-up commands, including source readiness passed/pending/total/check counts, manual storage backend telemetry, optional MinIO bucket CORS dashboard evidence, data-flow storage plan, data-flow query/retention budget, data-flow transition runbook, secret rotation, cluster network access review, Helm values hardening, commercial integration, commercial approval, chargeback closeout, optional enterprise auth smoke, enterprise auth JIT rollback, operations handoff package, support escalation handoff, and Kubernetes operations report sync run ids. It reads invocation and run-list JSON through explicit UTF-8.
- `../scripts/verify-operations-workflow-run-id-plan.ps1` - self-test workflow run id planning with plan-only saved run-list JSON handoff, GitHub REST API run-id command/query URL handoff, top-level browser URL/run-id input/recommended-command handoff, security finalizer required/missing run-id input and supplemental hint assertions, fixture-backed ready reports, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-artifact-collection-plan.ps1` - derive expected GitHub artifact names, `gh run download` commands, Security Evidence Finalizer dispatch input, structured `securityEvidenceFinalizerInputs[]` checklist, Operations Readiness Artifact Finalizer dispatch input, storage backend telemetry/optional MinIO bucket CORS dashboard evidence/monitoring threshold/data-flow storage plan/data-flow query-retention budget/data-flow transition runbook/secret rotation/cluster network access review/Helm values hardening/commercial integration/commercial approval/chargeback closeout/enterprise auth smoke or scope-out/enterprise auth JIT rollback/operations handoff package/support escalation handoff/Kubernetes operations report sync artifact input, data-flow query-plan summary handoff notes, and local artifact import commands from an invocation report plus workflow run ids while preserving source readiness passed/pending/total/check counts. It reads the invocation report JSON through explicit UTF-8.
- `../scripts/verify-operations-artifact-collection-plan.ps1` - self-test the artifact collection plan helper with missing and concrete workflow run ids, GitHub Actions run URLs, security finalizer input checklist, missing-run-id gating, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/finalize-operations-readiness.ps1` - orchestrate storage expansion, Kubernetes HA/DR, Kubernetes DR, security evidence finalization, and the combined operations readiness report from one guarded command; propagates `-PowerShellCommand` to child finalizers for Linux CI compatibility, passes the selected data-flow storage plan evidence path into the readiness gate, and reads readiness JSON through explicit UTF-8.
- `../scripts/verify-operations-readiness-finalizer.ps1` - self-test the operations readiness finalizer plan mode, secret masking behavior, and explicit UTF-8 reads for generated JSON/Markdown finalizer evidence.
- `../scripts/import-operations-readiness-artifacts.ps1` - import previously collected storage expansion, HA/DR, Kubernetes DR, IAM/RBAC, security evidence, storage backend telemetry, MinIO bucket CORS verification, monitoring threshold, data-flow storage plan, secret rotation, cluster network access review, Helm values hardening, commercial integration, commercial approval, enterprise auth smoke or scope-out, enterprise auth JIT rollback, operations handoff package, support escalation handoff, and Kubernetes operations report sync artifacts into the standard `.osmu-run/latest-*` readiness paths after validating their expected result values or, for CORS dashboard evidence, safe `passed|failed` reduced report shape. Validations include storage expansion finalizer typed zero failure/runner/RBAC/server-dry-run metadata, HA/DR readiness typed check/exit-code metadata, Kubernetes DR finalizer confirmed-restore/chronological-window/command-step metadata, IAM/RBAC finalizer typed zero failure/status/flag/verifier-step metadata, storage backend telemetry target metadata/source SHA/check-row/capacity metadata, MinIO bucket CORS no-raw-XML/no-secret result/check/count metadata, monitoring threshold target metadata/window/evidence-ref/check-row/alert-route/Grafana/tuning metadata, secret rotation target metadata/window/evidence-ref/check-row/core-rotation metadata, commercial integration target metadata/window/evidence-ref/integration/check-row/payment-adapter metadata, commercial approval product/approver/timestamp/evidence-ref/check-row/pricing-proposal metadata, enterprise auth generated/execution-mode/check-row/count/scope-out metadata, security finalizer typed zero failure/no-synthetic/promotion/check metadata, image signing GitHub OIDC keyless mode, version/commit ref tags, digest/signature booleans, and container scan commit-tag/SPDX SBOM typed metadata, enterprise auth `passed` typed integer summary counts or `scope-out` with typed accepted approval, sanitized monitoring threshold content with typed alert/route/Grafana/tuning/failure/check counts plus typed confirmation booleans and complete PASS check rows, direct data-flow target plan promotion only when `result=passed`, typed `pendingCount=0`, and MariaDB/dual-write query-plan summary `result=passed` with typed `failedCount=0`, data-flow transition runbook promotion only when target metadata, chronological review window, evidence refs, typed plan pending/check/query-plan summary, typed `failureCount=0`, all rehearsal/no-object/no-secret confirmations, and complete PASS check rows prove the target rehearsal, typed handoff confirmations, non-empty handoff evidence refs, strict typed operations readiness/convergence snapshot counts and sync booleans, typed handoff target evidence snapshots, synchronized handoff summary/check counts, complete handoff PASS check rows, and Kubernetes operations report sync promotion only when applied evidence has typed `failedCount=0`, ready convergence source metadata, a valid source SHA-256, and no kubeconfig or credential-shaped content, using strict UTF-8 reads for imported JSON/Markdown evidence inputs.
- `../scripts/verify-operations-readiness-artifact-import.ps1` - self-test the operations readiness artifact importer, including rejection of invalid failed evidence, weak image signing refs, weak container SBOM metadata, raw MinIO bucket CORS XML, string or missing monitoring threshold count fields, weak monitoring threshold check rows, weak data-flow transition runbook check rows, string-count passed enterprise auth evidence, string booleans or missing integer counts inside handoff package snapshots, weak handoff target evidence snapshots, weak support escalation handoff artifacts, MariaDB/dual-write data-flow plans missing query-plan evidence summary or carrying unsafe raw SQL/raw EXPLAIN/credential-shaped summary content, and explicit UTF-8 reads for generated/promoted artifact JSON evidence.
- `../scripts/write-operations-evidence-handoff.ps1` - stitch the latest operations readiness, evidence plan, guarded invocation, dispatch preflight, workflow run id, artifact collection, artifact import, and operations readiness finalizer reports into one current bottleneck plus readiness summary/count fields, stale downstream report count, dispatch preflight/workflow-run-id/artifact selected-action scope mismatch detection, downstream action-order freshness, browser-dispatch `browserDispatchChecklist[]` rows, GitHub REST API dispatch/run-id command hints, `postDispatchCommands[]` including direct browser run-id/run-URL artifact collection, and next-command handoff report. It reads downstream evidence JSON reports through explicit UTF-8.
- `../scripts/verify-operations-evidence-handoff.ps1` - self-test handoff selection for missing reports, stale invocation/dispatch-preflight reports, dispatch preflight/workflow-run-id/artifact selected-action scope mismatch, blocked invocation, browser-dispatch ready subset, artifact-finalizer-ready, operations-finalizer-missing, operations-finalizer-pending, readiness-ready but finalizer-missing states, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-operations-readiness-convergence.ps1` - summarize the latest handoff, readiness, operations finalizer, and Kubernetes operations report sync reports into a no-execute convergence report with readiness summary/count fields, current bottleneck, recommended command chain, browser-dispatch security-finalizer dependency notes lifted from the handoff checklist, stale handoff detection when readiness is newer than the handoff report, typed integer finalizer failed/gap gates, typed integer sync failed-count gate, sync source-result gate, sync freshness/stale gate against the latest handoff/readiness/finalizer inputs, the equivalent Kubernetes sync workflow command with optional data-flow storage plan/query-retention budget/runbook base64 inputs, and ready decision. It reads handoff/readiness/finalizer/sync JSON reports through explicit UTF-8.
- `../scripts/verify-operations-readiness-convergence.ps1` - self-test convergence reporting for missing handoff, stale handoff, action-required, browser-dispatch dependency-note lift, finalizer-required, sync-required, string finalizer/sync failed counts, missing finalizer/sync failed counts, not-ready sync source, fully ready states, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/sync-kubernetes-operations-reports.ps1` - validate and sync the latest operations readiness convergence report plus generated sync evidence into the Kubernetes `osmu-operations-reports` ConfigMap with sanitized data-flow query-plan summary, query/retention budget, and transition runbook checks, plan-only, server-dry-run, and explicit apply modes, reading convergence, sync evidence, and data-flow JSON inputs through explicit UTF-8.
- `../scripts/verify-kubernetes-operations-report-sync.ps1` - self-test Kubernetes operations report ConfigMap sync using a fake kubectl for plan, server dry-run, apply, data-flow query-plan summary, query/retention budget publish/skip, and unsafe summary rejection paths, using explicit UTF-8 reads for generated plan/apply/unsafe evidence fixtures.
- `../scripts/verify-kubernetes-operations-report-sync-live.ps1` - poll deployed dashboard readiness until it reflects the applied Kubernetes operations report sync evidence plus mounted data-flow storage plan/query-plan, query/retention budget, and transition runbook summaries without storing admin passwords or bearer tokens, reading local sync/data-flow evidence through explicit UTF-8.
- `../scripts/verify-kubernetes-operations-report-sync-live-self-test.ps1` - self-test the live sync verifier with dashboard fixtures for applied sync/data-flow storage plan/query-plan, query/retention budget, transition runbook, plan-only, stale sync states, and explicit UTF-8 reads for generated evidence fixtures.
- `../scripts/verify-kubernetes-operations-report-mount.ps1` - read-only live verifier that checks the operations report ConfigMap keys and confirms the backend Pod can read the mounted convergence, sync evidence, data-flow storage plan/query-plan summary, query/retention budget, and transition runbook files, comparing local JSON snapshots through explicit UTF-8 reads.
- `../scripts/verify-kubernetes-operations-report-mount-self-test.ps1` - self-test the ConfigMap and backend Pod mount verifier with a fake kubectl and explicit UTF-8 reads in generated fixtures and fake mounted file responses.
- `../.github/workflows/kubernetes-operations-report-sync-ci.yml` - manual GitHub Actions workflow for operations convergence report generation plus optional data-flow storage plan/query-retention budget/transition runbook restore and sanitized summary validation, Kubernetes ConfigMap sync plan, server-dry-run, and guarded apply evidence.
- `../scripts/finalize-iam-rbac-readiness.ps1` - finalize IAM/RBAC readiness evidence by running the application IAM/RBAC matrix verifier, Kubernetes RBAC matrix verifier, and optional backend focused RBAC tests or live `kubectl auth can-i` evidence.
- `../scripts/verify-iam-rbac-finalizer.ps1` - self-test the IAM/RBAC finalizer static and plan modes using strict UTF-8 reads for generated JSON/Markdown reports.
- `../scripts/write-container-security-evidence.ps1` - convert successful Trivy high/critical scan flags and backend/frontend SPDX SBOM files into `.osmu-run/latest-container-security-evidence.json` with SBOM byte size and SHA256 hashes, reading SBOM JSON through strict UTF-8.
- `../scripts/write-image-signing-evidence.ps1` - convert successful Cosign verification for backend/frontend version and commit-SHA image tags into `.osmu-run/latest-image-signing-evidence.json` with image manifest digests.
- `../scripts/verify-security-evidence-writers.ps1` - self-test the container security and image signing evidence writers with synthetic local artifacts and explicit UTF-8 reads for generated evidence JSON.
- `../scripts/finalize-security-evidence.ps1` - validate image signing and container scan/SBOM evidence from CI artifacts using explicit UTF-8 reads, reject synthetic evidence by default, promote passing evidence to the standard latest paths, and write `.osmu-run/latest-security-evidence-finalize.*`.
- `../scripts/verify-security-evidence-finalizer.ps1` - self-test the security evidence finalizer, promoted/rejected report reads, and its synthetic evidence rejection guard using explicit UTF-8 reads.
- `../scripts/verify-mvp-release-decision.ps1` - self-test release decision logic against synthetic lightweight and durable reports.
- `../scripts/write-monitoring-threshold-evidence.ps1` - record target Prometheus/Alertmanager/Grafana threshold review evidence from `infra/monitoring/alert-threshold-targets.yaml`, external rule/dashboard/route/baseline refs, and no-secret confirmations, reading the threshold target file through explicit UTF-8.
- `../scripts/verify-monitoring-threshold-evidence.ps1` - self-test the monitoring threshold evidence writer, including passed evidence, secret-shaped reference rejection, reversed review window rejection, missing required alert target rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/verify-ci-workflow.ps1` - verify GitHub Actions workflow drafts for lightweight CI, durable Docker MVP gate CI, real S3 client CI, container security CI, Browser E2E CI, storage expansion finalizer CI, Kubernetes HA/DR readiness CI, Kubernetes DR finalizer CI, operations readiness finalizer CI including data-flow storage plan/runbook evidence input, manual data-flow storage plan/runbook evidence CI, storage backend telemetry evidence CI, MinIO bucket CORS verification CI, cluster network access review evidence CI, Helm values hardening evidence CI, enterprise auth smoke CI, and finalizer `pwsh` command propagation.
- `../.github/workflows/storage-expansion-finalizer-ci.yml` - manual GitHub Actions workflow for storage expansion finalizer plan/live evidence collection. Live mode requires `OSMU_KUBECONFIG_BASE64` and uploads finalizer/RBAC/dry-run evidence artifacts plus optional storage backend telemetry evidence artifacts when `record_storage_backend_telemetry=true`.
- `../.github/workflows/kubernetes-ha-dr-readiness-ci.yml` - manual GitHub Actions workflow for Kubernetes HA/DR readiness plan/live evidence collection. Live mode requires `OSMU_KUBECONFIG_BASE64` and uploads `.osmu-run/latest-kubernetes-ha-dr-readiness.json`.
- `../.github/workflows/kubernetes-dr-finalizer-ci.yml` - manual GitHub Actions workflow for Kubernetes DR finalizer plan/live evidence collection. Live mode requires `OSMU_KUBECONFIG_BASE64`, uses `server_dry_run_only` by default, and requires explicit `confirm_restore=true` before confirmed restore or evidence submit can run.
- `../.github/workflows/operations-readiness-finalizer-ci.yml` - manual GitHub Actions workflow for combined operations readiness plan/live evidence collection. Live mode requires `OSMU_KUBECONFIG_BASE64`; optional `data_flow_storage_plan_json_base64`, `data_flow_query_retention_budget_json_base64`, and `data_flow_storage_transition_runbook_json_base64` restore passed data-flow transition evidence after rejecting unsafe query-plan/query-retention/runbook content, and the workflow uploads the combined readiness report plus selected storage, data-flow, HA/DR, DR, IAM/RBAC, and security evidence artifacts.
- `../.github/workflows/operations-readiness-artifact-finalizer-ci.yml` - manual GitHub Actions workflow that downloads evidence artifacts from previous storage expansion, HA/DR, Kubernetes DR, IAM/RBAC, security, storage backend telemetry, MinIO bucket CORS verification, monitoring threshold, data-flow storage plan, data-flow query/retention budget, data-flow storage transition runbook, secret rotation, cluster network access review, Helm values hardening, commercial integration, commercial approval, enterprise auth smoke, operations handoff package, and optional Kubernetes operations report sync workflow runs; optional `data_flow_storage_plan_json_base64`, `data_flow_query_retention_budget_json_base64`, and `data_flow_storage_transition_runbook_json_base64` import direct data-flow transition evidence through sanitized plan/query-plan/query-retention/runbook guards, then the workflow writes the operations readiness report and uploads the resulting artifact set.
- `../.github/workflows/manual-storage-backend-telemetry-evidence.yml` - manual GitHub Actions workflow that wraps `write-storage-backend-telemetry-evidence.ps1`, either decodes a prepared `OSMU_MINIO_ADMIN_INFO_JSON_BASE64` secret or uses `collection_mode=live` to run `mc admin info --json` against the target MinIO endpoint with temporary `MC_CONFIG_DIR`, removes raw input and mc config after the run, and uploads `storage-backend-telemetry-evidence-<run_id>`.
- `../.github/workflows/manual-minio-bucket-cors-verification.yml` - manual GitHub Actions workflow that wraps `verify-minio-bucket-cors.ps1`, either decodes a prepared `OSMU_MINIO_BUCKET_CORS_XML_BASE64` secret or uses `collection_mode=live` to run `mc cors info` against the target MinIO bucket with temporary `MC_CONFIG_DIR`, removes raw CORS XML and mc config after the run, and uploads `minio-bucket-cors-verification-<run_id>` for browser multipart upload readiness without treating it as AWS S3 parity.
- `../.github/workflows/manual-data-flow-storage-plan-evidence.yml` - manual GitHub Actions workflow that wraps `write-data-flow-storage-plan.ps1`, accepts target sizing/query-window/target p95 latency/retention/backfill/rollback/dashboard-cutover confirmations plus optional base64 sanitized MariaDB query-plan evidence, deletes the decoded query-plan input before upload, and uploads `data-flow-storage-plan-evidence-<run_id>`.
- `../.github/workflows/manual-data-flow-storage-transition-runbook-evidence.yml` - manual GitHub Actions workflow that wraps `write-data-flow-storage-transition-runbook-evidence.ps1`, accepts a base64 sanitized passed data-flow storage plan snapshot plus target backfill/dual-write or partition toggle/rollback/reconciliation/dashboard cutover/retention dry-run references, deletes the decoded plan input before upload, and uploads `data-flow-storage-transition-runbook-evidence-<run_id>`.
- `../.github/workflows/manual-monitoring-threshold-evidence.yml` - manual GitHub Actions workflow that wraps `write-monitoring-threshold-evidence.ps1`, accepts target Prometheus rule, Grafana dashboard, Alertmanager route, tenant baseline, and incident routing review references, and uploads `monitoring-threshold-evidence-<run_id>`.
- `../.github/workflows/manual-secret-rotation-evidence.yml` - manual GitHub Actions workflow that wraps `write-secret-rotation-evidence.ps1`, records target secret/certificate rotation evidence without secrets, and uploads `secret-rotation-evidence-<run_id>`.
- `../.github/workflows/manual-cluster-network-access-review-evidence.yml` - manual GitHub Actions workflow that wraps `write-cluster-network-access-review-evidence.ps1`, accepts a base64 non-secret access-review reference payload, deletes the decoded payload before upload, and uploads `cluster-network-access-review-evidence-<run_id>`.
- `../.github/workflows/manual-helm-values-hardening-evidence.yml` - manual GitHub Actions workflow that wraps `write-helm-values-hardening-evidence.ps1`, accepts a base64 non-secret verifier/hardening reference payload, deletes the decoded payload before upload, and uploads `helm-values-hardening-evidence-<run_id>`.
- `../.github/workflows/manual-commercial-integration-evidence.yml` - manual GitHub Actions workflow that wraps `write-commercial-integration-evidence.ps1`, accepts base64 payment-provider adapter readiness JSON, records target notification/payment/native-bridge readiness evidence without credentials or vendor-specific fixed SDK/schema processor claims, deletes decoded snapshot input before upload, and uploads `commercial-integration-evidence-<run_id>`.
- `../.github/workflows/manual-commercial-approval-evidence.yml` - manual GitHub Actions workflow that wraps `write-commercial-approval-evidence.ps1`, accepts base64 billing pricing proposal approval JSON, records final commercial/legal approval references without prices/contracts/license keys, deletes decoded snapshot input before upload, and uploads `commercial-approval-evidence-<run_id>`.
- `../.github/workflows/manual-operations-handoff-package.yml` - manual GitHub Actions workflow that wraps `write-operations-handoff-package.ps1`, accepts base64 operations readiness/convergence/data-flow storage plan/data-flow storage transition runbook/commercial/enterprise-auth evidence snapshots, requires explicit enterprise auth smoke snapshot review when production evidence is required, records target operations handoff package references without running external systems, deletes decoded temporary snapshot files, and uploads `operations-handoff-package-<run_id>`.
- `../.github/workflows/enterprise-auth-smoke-ci.yml` - manual GitHub Actions workflow for target IdP/LDAP enterprise auth smoke. It is plan-only by default, requires fixed GitHub secrets for live target checks, rejects token-bearing claim/JIT JSON input through the writer, deletes decoded temporary input files before upload, and uploads `.osmu-run/latest-enterprise-auth-smoke.*`.
- `../.github/workflows/iam-rbac-finalizer-ci.yml` - manual GitHub Actions workflow for IAM/RBAC finalizer evidence. It can run focused backend RBAC tests on Java 17 and optionally collect live `kubectl auth can-i` evidence when `run_live=true` and `OSMU_KUBECONFIG_BASE64` is provided.
- `../.github/workflows/security-evidence-finalizer-ci.yml` - manual GitHub Actions workflow that downloads image signing and container security artifacts from previous workflow runs, runs `finalize-security-evidence.ps1`, and uploads promoted security evidence artifacts.
- `../scripts/verify-openapi-contract.ps1` - parse and verify the machine-readable MVP OpenAPI contract and frontend API function coverage.
- `../scripts/verify-iam-rbac-matrix.ps1` - verify the IAM/RBAC matrix against core backend/frontend policy contracts using strict UTF-8 reads for docs, backend source, and frontend source.
- `../scripts/verify-kubernetes-rbac-matrix.ps1` - verify Kubernetes ServiceAccount, token automount, and storage expansion runner RBAC hardening contracts using strict UTF-8 reads for matrix docs, scripts, manifests, and Helm templates.
- `../scripts/verify-storage-expansion-rbac-auth.ps1` - collect live `kubectl auth can-i` evidence for the storage expansion runner ServiceAccount after Kubernetes RBAC is applied.
- `../scripts/verify-storage-expansion-server-dry-run.ps1` - collect live MinIO Tenant CRD, existing Tenant, and server-side dry-run evidence for storage expansion manifests, reading the source manifest through strict UTF-8 before building the effective manifest.
- `../scripts/finalize-storage-expansion.ps1` - one-command storage expansion finalization wrapper that runs RBAC authorization evidence, server-side dry-run evidence, optional backend dry-run/apply runner calls, and optional storage backend telemetry evidence recording with an explicit `-ConfirmApply` guard; supports `-PowerShellCommand` and records the selected command.
- `../scripts/verify-storage-expansion-finalizer.ps1` - self-test the storage expansion finalizer plan, apply guard, optional storage backend telemetry evidence link, and no-raw-admin-info evidence handoff using strict UTF-8 reads for generated JSON/Markdown reports.
- `../scripts/verify-kubernetes-ha-dr-readiness.ps1` - collect live Kubernetes HA/DR readiness evidence for backend/frontend replicas, StatefulSets, PodDisruptionBudgets, backup PVC/CronJobs, and restore Job server-side dry-run.
- `../scripts/run-kubernetes-backup-drill.ps1` - create one-off Kubernetes backup Jobs from the MariaDB/MinIO backup CronJobs, wait for completion, collect Job/Pod/log evidence, and write a backup drill report.
- `../scripts/prepare-kubernetes-restore-namespace.ps1` - render, dry-run, or apply a disposable Kubernetes restore target namespace and record namespace preparation evidence without copying secret values.
- `../scripts/verify-kubernetes-backup-artifacts.ps1` - run a read-only Kubernetes Job that checks restore PVC backup artifacts, metadata checksum, and MinIO mirror count/bytes before restore.
- `../scripts/run-kubernetes-restore-drill.ps1` - validate or run an isolated Kubernetes restore Job against a restore namespace and write restore drill evidence without copying secret values.
- `../scripts/run-kubernetes-dr-drill.ps1` - orchestrate the Kubernetes backup drill, optional DR bucket bootstrap, restore namespace preparation, optional DR bucket immutability preflight, artifact preflight, and restore drill sequence with a single wrapper evidence report.
- `../scripts/bootstrap-kubernetes-dr-bucket.ps1` - create or reuse an external S3-compatible DR bucket with object locking, versioning, and default retention, then write secret-free bootstrap evidence.
- `../scripts/verify-kubernetes-dr-bucket-immutability.ps1` - verify external DR bucket reachability, versioning, and default object-lock retention through `osmu-dr-transfer-secret` without copying secret values.
- `../scripts/transfer-kubernetes-backup-artifacts.ps1` - export/import a selected backup timestamp through external S3-compatible DR storage and write transfer evidence without copying secret values.
- `../scripts/verify-kubernetes-restore-smoke.ps1` - verify restored API health/login/bucket/object access plus optional real S3 client smoke and write restore smoke evidence.
- `../scripts/write-kubernetes-dr-evidence-request.ps1` - build or submit the admin restore evidence API request from Kubernetes DR wrapper evidence and artifact preflight logs, reading DR/backup/smoke JSON inputs through explicit UTF-8.
- `../scripts/finalize-kubernetes-dr-drill.ps1` - one-command Kubernetes DR finalization wrapper that runs DR drill orchestration, restore smoke, and evidence request generation/submission while masking admin password values in reports; supports `-PowerShellCommand`, records the selected command, and reads generated DR evidence request JSON through explicit UTF-8.
- `../scripts/verify-k8s-manifests.ps1` - verify the Kubernetes draft manifest set, secret handling, backup resources, HA PodDisruptionBudgets, and topology spread controls using explicit UTF-8 reads for base manifest, overlay, restore-example, and YAML placeholder scans.
- `../scripts/verify-mvp-release-artifacts.ps1` - check latest release JSON, durable gate/finalizer state, audit report, decision report, and release notes are synchronized using strict UTF-8 reads for source evidence and Markdown artifacts.
- `../scripts/verify-mvp-demo-package-notes.ps1` - verify MVP demo package notes preserve pilot handoff, release artifact paths, readiness status, and package-status alignment with MVP completion using strict UTF-8 reads for generated Markdown.
- `../scripts/start-mvp-demo.ps1` - start the best available current-machine MVP demo: Docker full stack when Docker is available, Java backend prototype when JDK 17+ is available, or frontend mock fallback.
- `../scripts/stop-mvp-demo.ps1` - stop the demo started by `start-mvp-demo.ps1` using the recorded selected mode.
- `../scripts/start-local-demo.ps1` - start full Docker demo stack with MariaDB, MinIO, Backend, and Frontend; waits for health endpoints and prints URLs.
- `../scripts/seed-local-demo.ps1` - seed a running full Docker demo with sample org, users, buckets, objects, lifecycle rules, and access key.
- `../scripts/verify-local-demo.ps1` - verify a running seeded full Docker demo through portal bundle, REST API, permissions, lifecycle, and seeded S3 access key smoke.
- `../scripts/stop-local-demo.ps1` - stop the Docker demo stack and optionally remove MariaDB/MinIO volumes with `-ResetData`.
- `../scripts/start-local-prototype.ps1` - start Docker-free in-memory backend and frontend; uses the shared Java 17+ helper and fails fast when Java is missing or target ports are already occupied.
- `../scripts/verify-prototype-gate.ps1` - run the complete lightweight prototype gate in one command.
- `../scripts/verify-lightweight-prototype.ps1` - smoke test a running Docker-free prototype.
- `../scripts/seed-lightweight-demo.ps1` - seed a running Docker-free prototype with demo org, users, buckets, objects, lifecycle rules, and access key.
- `../scripts/verify-lightweight-demo.ps1` - verify seeded portal data, frontend bundle, scoped permissions, tags, lifecycle, access key inventory, and S3 SigV4 access with the seeded key.
- `../scripts/write-data-flow-storage-plan.ps1` - write target sizing, p95 query latency budget, and readiness evidence for future data-flow partition/time-series transition without storing object keys, raw event messages, credentials, or raw EXPLAIN SQL; reads optional query-plan evidence through explicit UTF-8, and MariaDB partition/dual-write candidates can require a passed `latest-mariadb-query-plan-evidence.json` summary.
- `../scripts/verify-data-flow-storage-plan.ps1` - self-test the data-flow storage plan writer, including plan-required, passed query-plan fixture, failed query-plan fixture, credential-shaped input rejection cases, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-data-flow-query-retention-budget-evidence.ps1` - write target query-latency and retention-budget evidence after a passed storage plan, reading the storage-plan snapshot through explicit UTF-8 and rejecting raw SQL, object keys, raw messages, or credential-shaped content.
- `../scripts/verify-data-flow-query-retention-budget-evidence.ps1` - self-test data-flow query/retention budget evidence, passed storage-plan snapshot requirements, latency/retention typed budgets, timestamp ordering, no-raw-SQL/no-object-key confirmations, credential-shaped content rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../scripts/write-data-flow-storage-transition-runbook-evidence.ps1` - write target backfill, dual-write or partition toggle, rollback, reconciliation, dashboard cutover, and retention dry-run rehearsal evidence for a passed data-flow storage transition plan without storing raw SQL, raw EXPLAIN, object keys, raw event messages, or credentials, reading the storage-plan snapshot through explicit UTF-8.
- `../scripts/verify-data-flow-storage-transition-runbook-evidence.ps1` - self-test the storage transition runbook evidence writer, including passed evidence, secret-shaped reference rejection, reversed review window rejection, unsafe raw SQL plan rejection, and explicit UTF-8 reads for generated JSON/Markdown evidence.
- `../osmu-frontend/e2e/lightweight-demo.spec.js` - Browser E2E spec for stale session redirect, developer S3/API Key console, and admin storage portal click path. Run with `npm run test:e2e` from `osmu-frontend` after Playwright browsers are installed.
- `../osmu-frontend/playwright.config.js` - Playwright config. It reads `OSMU_PLAYWRIGHT_CHANNEL` so local runs can use installed Chrome or Edge when bundled Chromium download is unavailable.
- `../scripts/stop-local-prototype.ps1` - stop Docker-free prototype processes; use `-ForcePorts` to clean up stale child listeners on default ports.
- `../scripts/start-frontend-mock-demo.ps1` - start a Java-free frontend mock demo with Vite plus the Node mock API for UI/demo smoke; optional multipart threshold/part-size parameters support small Browser multipart fixtures without changing production defaults.
- `../scripts/verify-frontend-mock-demo.ps1` - start the frontend mock demo, reset mock API state, verify frontend shell, mock health, admin login, developer login/S3 config/access key flow, bucket/object flow, dashboard summary, and stop it.
- `../scripts/verify-browser-e2e-mock-demo.ps1` - start the frontend mock demo, reset mock API state, run Playwright Browser E2E against it, auto-select installed Chrome/Edge when `OSMU_PLAYWRIGHT_CHANNEL` is not set, optionally enable the small multipart pause/resume fixture with `-EnableMultipartFixture -TestGrep "pause and resume multipart"`, and stop the demo.
- `../scripts/stop-frontend-mock-demo.ps1` - stop frontend mock demo processes and optionally clear default ports with `-ForcePorts`.
- `../osmu-frontend/mock-api/server.mjs` - dependency-free Node mock API used by the Java/Docker-free frontend demo. `npm run mock:api:self-test` verifies its in-memory API fixtures, the mock-only `POST /api/mock/reset` state reset endpoint, and the current operations readiness mock chain with source `passed=82 pending=20`, source pending remediation count/list, evidence-plan remediation coverage, selected action 6, handoff `currentBottleneck=dispatch-ready-subset-browser`, browser dispatch URL/checklist for `container-security-ci.yml`, run-id parameter handoff, security finalizer dependency note coverage for `ImageSigningRunId`, supplemental workflow run-id hints, and handoff post-dispatch run-id commands.
- `../scripts/verify-browser-e2e-prototype.ps1` - start the Java in-memory backend and Vite frontend, seed the current operations convergence fixture with `dispatch-ready-subset-browser`/selected action 6 browser dispatch handoff plus security finalizer dependency notes, run backend API smoke plus Playwright Browser E2E, auto-select installed Chrome/Edge when needed, and stop the prototype.
- `../scripts/verify-browser-e2e-local-demo.ps1` - start the full Docker local demo, seed demo data and the current operations convergence fixture with `dispatch-ready-subset-browser`/selected action 6 browser dispatch handoff plus security finalizer dependency notes, verify REST/S3 demo smoke, run Playwright Browser E2E against the Docker frontend, auto-select installed Chrome/Edge when needed, and stop the Docker stack. `-EnableRealMultipartFixture -TestGrep "real MinIO multipart"` rebuilds the frontend with small multipart threshold/5 MiB part size, uses the seeded writable demo bucket, delays real MinIO PUT requests through Playwright `route.continue()`, verifies browser pause/resume against real MinIO rather than API mocks, and writes `.osmu-run/latest-browser-multipart-resume-evidence.json` plus `.md` without storing credentials, presigned URLs, object bytes, or raw request/response bodies.
- `../scripts/verify-durable-demo-preflight.ps1` - check durable demo prerequisites without starting containers; writes `.osmu-run/latest-durable-demo-preflight.*` with Docker, Compose, Node/npm, selected S3 client readiness, structured `nextAction`/`blockingActions[]` guidance for Docker daemon or S3-client blockers, and next commands for both standard and `-EnableRealMultipartEvidence` durable gate/finalize runs.
- `../scripts/verify-durable-demo-gate.ps1` - strongest local MVP demo proof. It starts the full Docker local demo, runs Browser E2E, runs Docker integration smoke, runs real S3 client smoke through host or Dockerized clients, writes `.osmu-run/latest-durable-demo-gate.*`, and stops the stack. `-EnableRealMultipartEvidence` runs the real MinIO browser multipart fixture without narrowing the rest of Browser E2E and requires `.osmu-run/latest-browser-multipart-resume-evidence.*`.
- `../scripts/verify-mvp-demo-readiness.ps1` - one-command current-machine demo readiness gate. It accepts `-JavaHome <jdk17>` and `-S3Client <auto|aws|boto3|aws-js|mc|docker-mc|all>`, runs Node prerequisites, static/frontend checks, mock API self-test, frontend mock demo smoke, mock Browser E2E, durable demo preflight, backend Gradle tests when available, backend-backed prototype Browser E2E when Java is available, durable Docker/MariaDB/MinIO/Browser/S3 client gate when Docker daemon is available, reads durable preflight evidence through strict UTF-8, and writes pending durable gates when proof is incomplete.
- `../scripts/verify-docker-integration.ps1` - full Docker integration smoke.
- `../scripts/verify-s3-compatibility-boundary.ps1` - verify S3 support stays scoped to replacement-layer compatibility, rejects overbroad English/Korean AWS S3 parity claims, and keeps README, root/dev-docs PRDs, API spec, feature inventory, status, roadmap intake gate, and release gates tied to `dev-docs/s3-compatibility.md`, using strict UTF-8 reads for all scanned text files.
- `../scripts/verify-s3-client-smoke.ps1` - S3 client compatibility smoke, including built-in SigV4 probes for object tagging, range GET, conditional requests, CopyObject, bucket tagging, multi-delete, AWS CLI checksum option smoke when host `aws` is available, boto3 checksum option smoke when Python+boto3 is available, AWS SDK JavaScript checksum option smoke when Node.js with `@aws-sdk/client-s3` is available, AWS SDK Java checksum option smoke when `OSMU_AWS_SDK_JAVA_CLASSPATH` points to AWS SDK Java v2 jars, bucket root listing plus object operations for host MinIO Client, and bucket root listing plus object operations for Dockerized MinIO Client via `-Client docker-mc`.

## Worklogs

- `worklog/main/worklog-main.md` - main branch historical worklog.
- `worklog/codex/frontend-backend-mvp/worklog-codex-frontend-backend-mvp.md` - current Codex branch worklog.

## Current Stack

- Backend: Spring Boot
- Frontend: Vue + Vite
- Metadata DB: MariaDB
- Object storage: MinIO
- Local infra: Docker Compose
- Kubernetes draft: `infra/k8s`
- API surface: REST API + S3-compatible API

## Current Prototype Gate

- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1 -SkipDocker -JavaHome <jdk17>`
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-k8s-manifests.ps1`
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-durable-demo-gate.ps1` after Docker Desktop is running
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-browser-e2e-local-demo.ps1` after Docker Desktop is running
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-docker-integration.ps1` after Docker Desktop is running
